{
  "name": "ip-changed-daemon",
  "version": "0.0.1",
  "description": "Monitor whether the IP on this device's network interface has changed (default to eth0) using Node.js OS networkInterfaces()",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Vodalok <visualcshape@gmail.com>",
  "license": "ISC"
}
